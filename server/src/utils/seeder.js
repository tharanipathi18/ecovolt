/**
 * Database seeder utility for Supabase PostgreSQL (Prisma).
 * Run with: npm run seed
 */

console.info('🌱 Database seeder for Supabase PostgreSQL (Prisma)');
console.info('   Run `npx prisma db seed` or `npm run seed` to populate data.');

process.exit(0);
